import datetime as dt #  set sart and end dates for data you want to pull
#Allows us to make plots
import matplotlib.pyplot as plt
from matplotlib import style # Allows editing of graphs
import pandas as pd
import pandas_datareader.data as web #replaces data.io

style.use('ggplot')

start = dt.datetime(2015, 1, 1)
end = dt.datetime.now()
df = web.DataReader("TSLA", 'yahoo', start, end)
df.reset_index(inplace=True)
df.set_index("Date", inplace=True)
#df = df.drop("Symbol", axis=1)

print(df.head())




